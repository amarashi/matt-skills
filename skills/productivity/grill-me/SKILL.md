---
name: grill-me
description: A relentless interview to sharpen a plan or design. Accepts an autonomy tier (assisted, autonomous).
disable-model-invocation: true
---

Run a `/grilling` session.

Pass through any autonomy tier the user named (e.g. `/grill-me autonomous`). This skill runs without a codebase, so there is no repo default to fall back on — an unstated tier means interactive.
